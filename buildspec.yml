version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11 
    commands:
      - pip install -r requirements.txt
  build:
    commands:
      - echo "Starting packaging the application with Python 3.11..."
artifacts:
  files:
    - '**/*'